---
export interface Props {
  currentPage: string;
}

const { currentPage } = Astro.props;

const currentSection = (path: string) => path.match(/(?<=\/[a-z]{2}\/)[^/]*/g)![0];

const activeTab = currentSection(currentPage);

const sections = [
  { name: "Learn", href: "/en/learn/getting-started/" },
  { name: "Reference", href: "/en/reference/createApplicationCommandHandler" },
];

const active = ["border-b-[2px]", "border-b-[#149ac9]", "text-[#149ac9]"];
---

<nav class="text-md mx-0 mb-2 flex w-auto justify-around border-b-[0.1px] border-b-gray-500">
  {
    sections.map((section) => (
      <a
        href={section.href}
        class={
          "flex w-full items-center justify-center py-2 px-6  " +
          (activeTab === section.name.toLowerCase() ? active.join(" ") : "")
        }
      >
        {section.name}
      </a>
    ))
  }
</nav>
